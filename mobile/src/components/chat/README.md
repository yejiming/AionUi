# chat components

- `screens/`: Chat pages and layout containers.
- `input/`: Input bar and composer-related components.
- `pickers/`: Modal/sheet pickers for mode/model/workspace/file.
- `message/`: Message rendering and tool call UI blocks.
- `workspace/`: Workspace grouping/presentation helpers.

Keep exports stable via `index.ts` in this directory.
