# Scripts layout

- `build/`: build and packaging preparation scripts
- `release/`: release pipeline scripts and artifact verification
- `dev/`: local development and diagnostics helpers
- `i18n/`: i18n type generation and validation
- `ops/`: automation and environment operation scripts

Keep entry points referenced by `package.json` under stable paths.
